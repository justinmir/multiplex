# M0.1: Create @app/core; move domain types; mockData re-exports

## Goal
Create a pure-TS workspace package `@app/core` holding the domain model, importable by all three existing packages (main, preload, renderer). Move shared type/interface declarations and pure helpers from `mockData.ts` into core. Rewrite mockData.ts to re-export types from core while keeping seed data local.

## Changes
1. **New package: `packages/core/`** — pure TS library with Vite lib build
2. **Move domain types** from `mockData.ts` → `packages/core/src/domain.ts`
3. **Rewrite `mockData.ts`** top to re-export from `@app/core`; keep `ago()` and seed arrays
4. **Add workspace dependency** on all three packages

## Validation
- `npm run validate` passes (0 errors)
- `npm run screenshot` renders identical UI
