# M4.1: Reply to agent in existing sessions — wire composer Send for ongoing conversations

## Problem
The Composer "Send" button only works when creating a new session — it does nothing for existing sessions. Additionally, the Enter key shortcut is limited to Cmd/Ctrl+Enter; users expect bare Enter to send messages (with Shift+Enter for multiline).

## Analysis of current state (M3.4 already wired most pieces)
- **SessionDetail.tsx**: `onSendMessage` prop exists and Composer's `onSend` callback routes correctly to it ✓
- **TaskView.tsx**: Passes `onSendMessage` through to SessionDetail ✓
- **AppShell.tsx**: `sendMessageToSession` handler creates user message + calls `startAgent` ✓
- **IPC/DataProvider**: `addMessage` and `startAgent` mutations exist from M3.4 ✓

## What needs fixing
1. **Enter key behavior** in Composer textarea (SessionDetail.tsx line 751) — change from `(metaKey || ctrlKey) && Enter` to bare Enter sends, Shift+Enter allows newline

## Changes
- `packages/renderer/src/app/components/SessionDetail.tsx` — update `onKeyDown` handler on textarea

## Validation
- `npm run validate` must pass (0 errors)
- `npm run screenshot` confirms UI renders without errors
