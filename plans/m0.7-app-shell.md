# M0.7: AppShell replaces mock App.tsx; remove ping probe

## Goal
Replace the direct-mock-data `App.tsx` with `AppShell.tsx` that sources data from DataProvider hooks. Remove the temporary `PingProbe` dev widget.

## Changes
- **NEW** `packages/renderer/src/shell/AppShell.tsx` — app shell component using DataProvider hooks
- **MODIFIED** `packages/renderer/src/main.tsx` — mount AppShell in DataProvider, remove PingProbe import
- **DELETED** `packages/renderer/src/lib/dev/PingProbe.tsx` — temporary dev widget
- **UNCHANGED** `packages/renderer/src/app/App.tsx` — kept on disk for Figma re-sync reference

## Data flow
```
AppShell → useProjects() / useStandaloneSessions() → DataContext → MockDataSource → mockData arrays
```

## Validation
- `npm run validate` passes (typecheck + lint + build)
- `npm run screenshot` renders without console errors
